<?xml version="1.0" encoding="utf-8"?>
<!-- ScrollView is a safety net, not the primary fix; the sizes/margins below are
     tuned to fit a normal landscape phone screen without scrolling at all. Content
     previously sized for a taller portrait screen ran off the bottom in landscape. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0F1E"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp">

        <!-- SCOUT wordmark -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:letterSpacing="0.15"
            android:text="SCOUT"
            android:textColor="#FFFFFF"
            android:textSize="30sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="10dp"
            android:letterSpacing="0.25"
            android:text="AI COMPANION APP"
            android:textColor="#9BBEFF"
            android:textSize="11sp" />

        <!-- Scout face icon -->
        <ImageView
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:layout_marginBottom="10dp"
            android:src="@mipmap/ic_launcher" />

        <!-- Animated rotating message — clip so slide animation stays inside bounds -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:clipChildren="true"
            android:layout_marginBottom="14dp">

            <TextView
                android:id="@+id/downloadMessage"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:lineSpacingMultiplier="1.2" />

        </FrameLayout>

        <!-- Progress bar -->
        <ProgressBar
            android:id="@+id/downloadProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginBottom="8dp"
            android:max="100"
            android:progress="0"
            android:progressTint="#9BBEFF"
            android:progressBackgroundTint="#1E2A45" />

        <!-- Percentage + size/time row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/downloadSizeText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="#6B7FA8"
                android:textSize="13sp"
                android:text="" />

            <TextView
                android:id="@+id/downloadPercent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#9BBEFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:text="0%" />

        </LinearLayout>

        <!-- Real feature tips, shown only during the Loading phase, not while
             Downloading; gone by default. ModelDownloadActivity.kt reveals this
             once the download (if any) finishes and loading into memory begins. -->
        <LinearLayout
            android:id="@+id/tipContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_tip_callout"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="gone">

            <TextView
                android:id="@+id/tipIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="10dp"
                android:text="💡"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/tipText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="#C7D3EE"
                android:textSize="13sp"
                android:lineSpacingMultiplier="1.2" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>
